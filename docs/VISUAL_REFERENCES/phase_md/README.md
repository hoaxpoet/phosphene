# Phase MD — Milkdrop Preset Visual References

Reference folders for Milkdrop-originated presets land here when Phase MD.1+
identifies which presets ship. Each subfolder follows the same structure as the
production presets in `docs/VISUAL_REFERENCES/`.

See `docs/ENGINEERING_PLAN.md §Phase MD` for the Milkdrop ingestion roadmap.
The `PhospheneTools/MilkdropTranspiler` (planned) will be the first Phase MD increment.
