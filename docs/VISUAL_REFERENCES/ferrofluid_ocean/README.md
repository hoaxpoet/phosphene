# Visual References — Ferrofluid Ocean

**Family:** fluid
**Render pipeline:** ray_march + post_process
**Rubric:** full (gated by V.6 certification)
**Last curated:** 2026-05-13 by Matt (V.9 redirect; see DECISIONS.md D-124)
**Last amended:** 2026-05-14 by Claude post-Session-4.5 Phase A — technical references reconciled to D-126 (matID == 2 lighting paradigm changed from Cook-Torrance per-light to mirror-reflects-procedural-sky). Implementation pointers and audio routing reconciled to current code (Phase 0 reverted three Session 4 decoration layers; Phase A switched the spike-field height function from regular Voronoi to Quilez smooth Voronoi). Image annotations preserved; only their hard § references to retired code paths were updated.

> **Stylization caveat for any session reading this folder.** Every photograph in this folder was sourced because it teaches a *specific* trait — material behavior, scale, lattice character, swell motion, palette anchor, atmosphere, or lighting mechanic. None of them is a faithful rendering of "Ferrofluid Ocean as it should appear." Read each image only for the trait its annotation calls out, and *actively disregard* every other property of the image — particularly: studio gel lighting colors; the literal cause of any natural phenomenon depicted (auroras, storms, sun-on-water); foreground objects (lotus veins, foam crests, beach rocks, lightning bolts, mountain silhouettes, lake bottoms, celestial point sources, distant shorelines); any palette that conflicts with §10.3.5's "distant fog cools to dark purple" intent. The preset's hue comes from (a) mood-valence-tinted base sky (D-022 via `scene.lightColor.rgb`) and (b) audio-reactive aurora bands in the procedural sky that the substrate mirror-reflects (D-126; the §5.8 stage-rig drives the bands' palette / intensity / orbit, but the bands live in the sky function, not as direct lights cast onto the surface). Direct-lit albedo is fixed by §4.6: `float3(0.02, 0.03, 0.05)`. Per-image disregard guidance follows the convention promoted to rule level in **D-065 §(c)** (`SHADER_CRAFT.md §2.1` step 2).

## Reference images

Files in this folder. Sub-letters (`02b`, `02c`, `03b`) denote supporting images that isolate a distinct trait of the same scale tier. Format: `NN_<scale>_<descriptor>.jpg` where `<scale>` is one of `macro` / `meso` / `micro` / `specular` / `atmosphere` / `lighting` / `palette` / `silence` / `anti` and `<descriptor>` is a 2–4 word lowercase_underscored descriptor. See `../_NAMING_CONVENTION.md`. References should be ≤ 500 KB each; crop and compress before committing. The folder exceeds the §2.1 "3–5 references" target under the composite-preset allowance in **D-065 §(a)** — Ferrofluid Ocean's traits are not contained in any single photographable subject, and each image isolates a distinct trait.

| File | Annotation (what to learn — and what to disregard) |
|---|---|
| `01_macro_ferrofluid_at_swell_scale.jpg` | **Trust:** spike-field topology at ocean-replaced-with-ferrofluid scale (not lab-tabletop scale); spike density and elaboration as a continuous expanse; the value range (near-black substrate, sharp specular catches on spike edges); the framing scale — this image teaches "a portion of an ocean" from 5–10 feet above the surface, not a horizon vista. **The hero macro reference under the V.9 redirect.** **Aggressively disregard:** the apparent grid regularity in this image — the preset's lattice uses Quilez smooth Voronoi (Phase A 2026-05-14) which has C¹-continuous height variation across cell boundaries and inherent non-uniformity from the cell-hash offsets; this image teaches scale and density, *not* periodicity. Also disregard: any slight bokeh suggesting a macro-lens depth-of-field (the preset is rendered at infinite focus); any specific lighting direction implied (under D-126 the surface mirror-reflects a procedural sky with §5.8-rig-driven aurora bands, not direct illumination from this reference's direction). |
| `02_meso_lattice_defects.jpg` | **Trust:** hexagonal close-pack of spike centers with visible per-cell variation — no two spikes identical in spacing or tilt. The preset's smooth Voronoi (Quilez, Phase A) produces this character naturally from the cell-hash position offsets; `02_meso_*` is the visual target for that natural non-uniformity. **Disregard:** the red + blue studio gel lighting; D-022 mood-tinted base sky and the audio-reactive aurora bands the surface reflects (per D-126) provide hue, not gels. |
| `02b_meso_swell_motion_dark_water.jpg` | **Trust:** macro swell motion geometry on a dark body of liquid — crest-to-trough wavelength, the gentle organic non-periodicity of the wave train, the way successive crests read as a coherent moving body rather than independent ripples. This is the §10.3.1 Gerstner-swell reference under the V.9 redirect: large-amplitude macro motion lives underneath the spike lattice, and this teaches what that motion should look like at the meso scale. **Disregard:** the fact that this is water and not ferrofluid (the material is wrong; only the motion geometry is right); any warm tonality at the top of the frame (sky lighting, not surface property); any foam crests at the very top (ferrofluid does not foam). |
| `02c_meso_excited_water_specular.jpg` | **Trust:** peak-energy ripple density on an excited body of liquid; the spatial frequency and distribution of sharp specular pinpoints catching directional light across the surface. Useful for understanding the preset's *excited state* at meso scale — many high-frequency catches scattered across a darker base, dense without becoming chaotic. **Disregard:** the warm sun color of the specular catches (under D-126 those are aurora-band reflections in the substrate, palette-driven, not solar); the bright overall exposure (the preset stays dark even at peak energy — only the catches are bright); the underlying water material (it's still ferrofluid). |
| `03_micro_spike_surface.jpg` | **Trust:** surface-scale grain along a single spike ridge; near-mirror metal between micro-features. (This is a tight crop of the same source as `04_*` — same lighting setup, same stylization caveat.) **Status:** the §10.3.3 micro-normal perturbation that originally anchored this image was reverted in Session 4.5 Phase 0 (Failed Approach #62 — the perturbation destroyed the substrate's mirror identity by scattering the thin-film specular). Image retained pending re-evaluation in Phase B or later; do not implement the micro-normal feature without explicit re-authorization. **Disregard:** the purple gel. |
| `03b_micro_droplet_beading.jpg` | **Status:** the §10.3.3 Cassie-Baxter droplet feature this image anchored was reverted in Session 4.5 Phase 0 (Failed Approach #62 — droplets had no load-bearing musical role and read as foreign decoration). Image retained pending re-evaluation in a future increment; do not re-implement droplets without explicit re-authorization that articulates each droplet's musical role per Authoring Discipline. If droplets do return, **trust ONLY** the Cassie-Baxter bead-on-tip geometry from this image. **Aggressively disregard:** the radial vein pattern emanating from the leaf center is **NOT** a directive about spike arrangement — spike-center distribution is hex-pack via smooth Voronoi (Phase A) per §4.6 and `02_meso_*`, never radial-from-a-hub. Also disregard: the green color, the leaf substrate, the leaf shape. |
| `04_specular_razor_highlights.jpg` | **Trust:** razor-sharp, near-mirror specular highlights running along each spike ridge; pitch-black troughs; the §4.6 `roughness = 0.08, metallic = 1.0` material character; **AND** the way the purple beam interacts with the spike geometry — saturated rim highlights where the beam catches spike edges, near-black substrate even at the brightest beam location, chromatic content present only in the reflections (not in the underlying surface tone). **Dual-anchor reference**: hero for both specular character (§10.3.4) and — under D-126 — the aurora-band content the substrate reflects from the procedural sky. Read the purple beam as one *frozen instant* of a moving colored aurora band in the sky that the mirror surface reflects (orbit advances → band sweeps in azimuth → reflections sweep across the substrate). **Disregard:** the static composition (in the preset the aurora bands move, this is one frame); the specific purple hue (palette-driven by `vocals_pitch_hz` per §5.8, not fixed at this color); the macro-lens framing depth (the preset's framing is at ocean-portion scale per `01_*`, not tabletop macro). |
| `05_anti_chrome_blob_AIGEN.jpg` | NOT this. Generic smooth-chrome blob with no Rosensweig spike character. The single most likely Claude Code shortcut: skipping the §4.6 hex-tile spike field and rendering a wavy reflective surface instead. *Every* trait of this image is anti — material is too uniform, surface too smooth, no detail cascade, no spike topology. **Note:** This image is AI-generated under the **D-065 §(b)** carve-out for the anti-reference slot. See Provenance for the replacement plan. |
| `06_palette_dark_metallic.jpg` | **Trust ONLY:** the lower-frame dark grit as a base palette / value swatch — the deep blue-black with cool undertone that anchors `mat_ferrofluid`'s `albedo = float3(0.02, 0.03, 0.05)`. **Aggressively disregard:** any white foam visible in the frame is **NOT** representative of any part of the preset's between-spike base — that surface is mirror-metal per §4.6, *not* foam-capped water. (Recommended: crop the foam out entirely before committing.) |
| `07_atmosphere_dark_purple_fog.jpg` | **Trust:** the deep purple-to-near-black gradient as a static atmospheric tint; atmosphere as material, sitting uniformly across the frame rather than originating from a discrete light source. This is the §10.3.5 "distant fog cools to dark purple" anchor — the closest available real-photograph reference for "purple fog as a static scene property." **Aggressively disregard:** the source image is from a thunderstorm; any sense of *active weather* (lightning, dramatic flicker, rolling cloud movement) is **NOT** what the §10.3.5 fog does. Fog is static volumetric tint, not a dynamic phenomenon. The lightning thread in the upper-left of the source is cropped out before commit; if any trace remains, ignore it. |
| `08_lighting_aurora_over_dark_water.jpg` | **Trust:** diffuse colored light moving across a dark reflective body of liquid; the way the body picks up the chromatic content without the substrate ever turning bright; soft beam-like gradients that *flow* rather than strobe; the implication of continuous motion frozen at one instant; the doubled-aurora-in-reflection composition showing what colored light over a reflective surface looks like at landscape scale. **Under D-126 (V.9 Session 4.5 Phase A) this is the literal canonical reference for the preset's lighting paradigm:** the surface mirror-reflects a procedural sky that contains aurora bands driven by the §5.8 rig. The doubled-aurora-in-reflection composition is mechanically what the preset produces. **Aggressively disregard:** the bright celestial point in the upper-right is **NOT** a directive about preset lighting — the aurora bands are continuous diffuse gradients in the sky function, *not* point sources with pillar reflections. (Point-source pillar reflections are a documented failure mode the D-126 paradigm pivot was authored to eliminate; see anti-references and Failed Approach #61.) Also disregard: the literal cause of the aurora; the specific green hue (palette is driven by `vocals_pitch_hz` per §5.8, not anchored to this hue); any horizon-line silhouette of distant rocks (the preset is at ocean-portion scale, not landscape vista); the visible star field (the preset has no celestial content). |
| `10_silence_calm_body.jpg` | **Trust:** calm-state surface character — gentle low-amplitude ripple, dark restful palette, sense of a coherent body of liquid at rest. The V.9 redirect makes the silence state a real visual destination (not just "spikes shrink to 10%"); at total silence the surface should read like a calm dark body of liquid this way, with the spike lattice fully collapsed and only the macro swell breathing through. The §10.3 / D-019 silence anchor. **Aggressively disregard:** the visible lake bottom showing through at the bottom of the frame is **NOT** a directive about preset transparency — the preset's surface is opaque ferrofluid, not clear water. Also disregard: any warm hint in highlights (the silence state stays cool/dark); the slight greenish algal tint near the bottom; any sense of "shore" or shallow water (the preset has no edges or shallows). |

## Mandatory traits (per SHADER_CRAFT.md §12.1)

For this preset specifically, the following implementations are mandatory:

- [ ] **Detail cascade:**
  - **macro** = smooth-Voronoi spike lattice (`voronoi_smooth` from `Utilities/Texture/Voronoi.metal` — Quilez exp/log soft-min) with `spike_height ∝ stems.bass_energy_dev` per §4.6 + §10.3.1, *composed on top of* a Gerstner-wave macro displacement field driving the base height of the lattice (4 superposed Gerstner waves; amplitude routed from arousal + drums_energy_dev accent per the Audio routing notes below); the *scope* of the lattice extends to an ocean-portion horizon, not a contained dish (see `01_*` for scale, `02b_*` for swell motion geometry). Phase A switched from regular Voronoi (`voronoi_f1f2`) to smooth Voronoi to eliminate cell-boundary normal-flip artifacts; the C¹-continuous height function is the canonical technique per Robert Leitl's audio-reactive ferrofluid project.
  - **meso** = per-cell variation comes from the smooth Voronoi's cell-hash offsets (Quilez's `voronoi_cell_offset`); the smooth-min blend gives organic non-uniformity by construction. The domain-warp + drums-flow approach originally specced here is retired; the smooth-Voronoi approach produces the `02_*` lattice-defect character without an explicit warp seed.
  - **micro** = **deferred / not currently implemented.** The `fbm8(p * 15.0)` normal perturbation and hash-lattice micro-droplets originally specced here were implemented in Session 4 Phase A and reverted in Session 4.5 Phase 0 (Failed Approach #62 — destroyed the substrate's mirror identity, no load-bearing musical role). Phase B may revisit; do not re-implement without explicit re-authorization that articulates the musical role per Authoring Discipline.
  - **specular** = `mat_ferrofluid` from §4.6 (`albedo = float3(0.02, 0.03, 0.05)`, `roughness = 0.08`, `metallic = 1.0`) with thin-film F0 from `rm_thinfilm_rgb` (renderer-local port of `Utilities/PBR/Thin.metal`'s `thinfilm_rgb`); 220 nm baseline thickness ± arousal × 40 nm range per D-126 / current `FerrofluidParams`. Anisotropic roughness aligned to spike axis (§10.3.4) is aspirational / not currently implemented; revisit in Phase B if cert review demands.
- [ ] **Hero geometric function:** `voronoi_smooth(p.xz, scale, k)` from `Utilities/Texture/Voronoi.metal` (Quilez's exp/log soft-min over 9 neighbor cells; scale 4.0, k 32, kSpikeRadius 0.6 scaled-space units → wall-to-wall pyramid coverage). Reference: https://iquilezles.org/articles/smoothvoronoi/. **Gerstner-wave displacement is implemented preset-level** per the §4.14 `mat_ocean` recipe comment convention; no Gerstner utility exists in the V.1 noise tree, and V.9 implements the Gerstner sum (4 superposed waves) inline as part of its `sceneSDF`.
- [ ] **Material count and recipes:** `mat_ferrofluid` (§4.6, primary spike material) + **thin-film interference layer** via `rm_thinfilm_rgb` in `Shaders/RayMarch.metal` (renderer-local port of `Utilities/PBR/Thin.metal`'s `thinfilm_rgb`; promoted to per-preset mandatory under the V.9 redirect; provides the iridescent shift in aurora-band reflections that reads as "magnetic fluid" rather than chrome). `mat_ferrofluid` from `Shaders/Utilities/Materials/` (V.3). Note: `mat_ocean` (§4.14) was originally specced for the between-spike base but under D-126 matID == 2 bypasses the standard lighting tail entirely and reads only from the procedural sky function — `mat_ocean`'s body-of-liquid depth recipe doesn't apply in the current pipeline; the dark substrate identity comes from the §4.6 albedo + thin-film F0 × sky reflection mechanism. Plasma-family exemption does **not** apply.
- [ ] **Audio reactivity (D-026 deviation primitives only):**
  - `stems.bass_energy_dev` → spike height (Rosensweig field strength) — **implemented in current code**
  - Arousal (5s low-pass) + `stems.drums_energy_dev` accent → macro Gerstner swell amplitude (arousal sets sustained baseline at ~70%, drum energy adds short-period crest emphasis at ~30%) — **implemented**
  - Arousal → §5.8-rig orbit speed → aurora-band azimuth sweep rate in the sky function — **implemented**
  - `stems.vocals_pitch_hz` normalized inline (perceptual log-scale over 80 Hz–1 kHz; confidence-gated at `stems.vocals_pitch_confidence >= 0.6`) with `stems.other_energy_dev` fallback → §5.8-rig palette phase → per-band aurora hue in the sky function — **implemented**
  - `stems.drums_energy_dev` → §5.8-rig intensity envelope (150 ms τ smoother) → per-band aurora brightness in the sky function — **implemented**
  - Arousal → thin-film thickness baseline ± 40 nm range → iridescent shift in aurora-band reflections — **implemented** (matID == 2 branch)
  - **Deferred / not currently implemented:** beat-surface ripple from `stems.drums_beat` (omitted in Phase A per Failed Approach #4 + `check_drums_beat_intensity.sh` — beat as primary motion is the documented anti-pattern); spike sharpness from `stems.vocals_energy_dev` (no surface-tension parameter in current spike profile); rotational flow direction from `stems.other_energy_dev` (no domain-warp seed in current geometry — smooth Voronoi provides non-uniformity without it). Phase B may revisit these if cert review demands; the omissions are intentional, not oversights.
  - **No absolute-threshold patterns** (`f.bass > 0.22`, etc.) per D-026 — explicitly disallowed.
- [ ] **Silence fallback (D-019 warmup):** at `totalStemEnergy == 0` the preset must still render. `fo_spike_strength` crossfades from `f.bass_att_rel` proxy to `stems.bass_energy_dev` stem via `smoothstep(0.02, 0.06, totalStemEnergy)`. At true silence: **spike lattice fully collapses** (the `fo_ferrofluid_field(p, 0)` early-exits to 0); macro Gerstner swell continues at minimum amplitude (gentle restful surface per `10_*`); base-sky reflection visible (dark-purple gradient × D-022 mood tint); aurora bands continue at minimum intensity (rig per-light intensity = `baseline × floor_coef ≈ 2.0` at silence per §5.8 defaults) so subtle chromatic content rides on top of the base sky. **Calm, non-black, non-static** at the lower bound.
- [ ] **Performance ceiling:** **7.0 ms p95 at 1080p Tier 2** (bumped from 6.0 ms under the redirect to absorb the §5.8 stage-rig evaluations and Gerstner swell additions). Tier 1 cost TBD — Session 5 cert review.
- [ ] **Hero reference image:** `04_specular_razor_highlights.jpg`. The single most-important match: spike-ridge highlights must be narrow and near-mirror; troughs must read pitch-black; albedo must be nearly zero on the surface itself with all visible color coming from the sky-reflection × thin-film F0 mechanism per D-126.

## Expected traits (per §12.2 — at least 2 of 4)

- [ ] **Triplanar texturing on non-planar surfaces** — applicable: only if §10.3.3 surface-scale detail is re-introduced. Currently deferred; the Session 4 micro-detail features were reverted in Phase 0. Revisit if Phase B re-authors micro-detail.
- [ ] **Detail normals** — applicable: only if §10.3.3 micro-detail is re-introduced. Currently deferred (see triplanar note above). The `fbm8(p * 15.0)` micro-detail normal originally specced here was reverted in Phase 0.
- [ ] **Volumetric fog or aerial perspective** — applicable: yes. §10.3.5 specifies "distant fog cools to dark purple." For matID == 0 / matID == 3 paths, `scene_fog` + D-022 mood-tint on `lightColor.rgb` drives this through `rm_finishLightingPass`. For matID == 2 (current Ferrofluid Ocean substrate path), the procedural sky function `rm_ferrofluidSky` carries the atmospheric tint via its base-sky gradient × `scene.lightColor.rgb`, and the matID == 2 branch mixes toward `rm_ferrofluidBaseSky` at the zenith for atmospheric depth (per D-126). The fog feature is present in both paths; the implementation differs.
- [ ] **SSS / fiber BRDF / anisotropic specular** — applicable: aspirational. §10.3.4 calls for anisotropic reflection aligned with spike axes. Not currently implemented; revisit in Phase B or Session 5 cert review if isotropic specular reads as missing the razor character. (See `04_*` for the specular target; see `08_*` for how a diffuse colored gradient reads across a dark reflective body at landscape scale.)

All four expected traits apply. Target ≥ 3 of 4 implemented for headroom against the rubric.

## Strongly preferred traits (per §12.3 — at least 1 of 4)

- [ ] **Hero specular highlight visible in ≥60% of frames** — applicable: yes. The §4.6 material at `roughness = 0.08` × thin-film F0 produces near-mirror highlights when the substrate normal aligns the reflection vector with an aurora band's central direction. The §5.8 rig's orbit altitude / radius position the bands at ~33° from zenith — geometry that the spike-slope normals naturally redirect toward (per D-126). This should be free given the lighting recipe.
- [ ] **Parallax occlusion mapping** — applicable: no. POM is for surface-displacement on flat-ish surfaces (bark, concrete walls). The spike lattice already provides 3D displacement via the SDF; layering POM on top would compound cost without visible benefit at this material's roughness.
- [ ] **Volumetric light shafts or dust motes** — applicable: optional. Could add faint above-surface dust motes catching the aurora bands as atmospheric texture, but the scene is intentionally dark-dominant — defer unless the scene otherwise reads empty.
- [ ] **Chromatic aberration or thin-film interference** — *promoted to mandatory under the V.9 redirect* (see Mandatory traits → Material count and recipes above). Retained as a list entry for traceability; not double-counted in the §12.3 minimum.

Hero specular alone satisfies the §12.3 minimum; thin-film's promotion to mandatory leaves three remaining §12.3 candidates with only hero specular applicable.

## Anti-references (failure modes specific to this preset)

What a Claude Code session is most likely to produce by accident; what this preset must NOT look like:

- **Generic chrome metaballs / smooth reflective blobs** with no spike character. The single most likely failure mode if the session shortcut to writing a Beer's-law or simple-displacement surface instead of implementing the §4.6 hex-pack spike field via smooth Voronoi. See `05_anti_chrome_blob_AIGEN.jpg`.
- **Perfectly regular hexagonal lattice.** A risk if the session reverts the smooth-Voronoi cell-hash offsets (which provide the natural per-cell position variation) or reads `01_*`'s apparent regularity literally. The Quilez `voronoi_smooth` utility's `voronoi_cell_offset` hash gives organic non-uniformity by construction; do not "regularize" the cell positions for aesthetic predictability.
- **Radial / hub-and-spoke spike arrangement.** A specific risk if the session over-reads the lotus-leaf vein structure in `03b_*`. Spike centers are hex-pack via smooth Voronoi, not radial. The lotus is in the folder pending re-evaluation of micro-droplet behavior in a future increment (Phase 0 reverted the droplet feature).
- **Foam-capped water as the between-spike base.** A specific risk if the session over-reads the upper portion of `06_*`, the foam crests at the top of `02b_*`, or pattern-matches generic "ocean" imagery. The §4.6 between-spike surface is *mirror-metal*, not water with surf; ferrofluid does not foam.
- **Active weather effects in the fog.** A specific risk if the session over-reads `07_*` and reproduces lightning, dramatic flicker, or rolling cloud movement. The §10.3.5 fog is a static atmospheric tint — tone, not phenomenon.
- **Static surface / missing Gerstner swell.** A specific risk if the session implements the spike-lattice height modulation but forgets the underlying macro Gerstner displacement. The surface must move as a coherent body of liquid, not have spikes pulsing on a flat plane. `02b_*` is the reference for what's missing; absence of swell motion at any time other than total silence is a documented failure.
- **Aurora reads as a club rig** rather than a continuous physical phenomenon. Moving colored bands that strobe on beat onsets in saturated party colors, or band motion that is jittery and beat-quantized rather than continuous and slowly orbital. The §5.8 rig (driving the aurora bands per D-126) produces continuous palette-driven hue rotation and slow orbital motion (orbit speed ← arousal, hue ← `vocals_pitch_hz` normalized inline with `other_energy_dev` fallback, intensity ← `drums_energy_dev` envelope) — never beat-strobed direction changes or onset-edge intensity pulses. See `04_*` for the *quality* of aurora-on-ferrofluid (one frozen instant of mirror-reflected sky); the failure mode is band *behavior* (jittery on-beat strobes, oversaturated party palette).
- **Point-source light reflections / vertical pillar reflections on the surface.** A specific risk if the session reads the bright celestial point in `08_*` as a directive about preset lighting. The aurora bands in the procedural sky are continuous diffuse gradients sampled at the reflection vector (D-126), not sharp point reflections forming vertical pillars (like a moon or lighthouse reflecting in a calm lake). Pillar reflections are exactly the wrong reading and were the failure mode the D-126 paradigm pivot eliminated. See Failed Approach #61.
- **Implementing the §5.8 rig as direct Cook-Torrance point lights with inverse-square attenuation.** Failed Approach #61. At any reasonable physical orbit distance (altitude 6 m / radius 4 m, surface-to-light ~7 m), inverse-square attenuation gives ~0.02× per beam → invisible beams against the IBL the same near-mirror surface reflects. D-126 retired this paradigm in favor of mirror-reflects-procedural-sky: the rig outputs drive aurora bands IN THE SKY, not direct illumination ON THE SURFACE.
- **Calm state still showing spikes.** A specific risk if the session ties spike height to `bass_energy_dev` but doesn't crossfade the lattice itself out at total silence. The destination silence state is `10_silence_calm_body.jpg` — a smooth body of liquid with macro swell only, *no spike lattice present*. "10% baseline" is the prior spec and is retired under the redirect; `fo_ferrofluid_field` returns 0 when `fieldStrength <= 0` so the lattice fully collapses.
- **Soft / diffuse highlights.** Anything above `roughness = 0.15` reads as plastic. The §4.6 `roughness = 0.08` is non-negotiable for the "razor-sharp" character. See `04_specular_razor_highlights.jpg`.
- **Saturated direct-lit color.** The preset is not red, not purple, not any color in albedo. All visible color comes from base-sky reflection + D-022 mood-tint on `scene.lightColor.rgb` + aurora-band reflections in the sky function. Direct albedo is `float3(0.02, 0.03, 0.05)`. The colored gels in `02_*` and `04_*` are studio photography, not palette directives.
- **Beat-driven primary motion.** Per `CLAUDE.md §Audio Data Hierarchy`, beat onsets are accents only; continuous energy (`*_energy_dev`) is the primary driver. A preset where spike height pumps only on `drums_beat` rising edges fails the "feels locked to the music" test. Same rule extends to swell amplitude (arousal-baseline, drums-accent) and aurora-band intensity (`drums_energy_dev` envelope, not `drums_beat`) — energy envelopes, not onset edges. `Scripts/check_drums_beat_intensity.sh` enforces.
- **Decoration layers without articulated musical role.** A specific risk recurrence of Failed Approach #62 — Session 4 added Cassie-Baxter droplets, meso domain warp, and matID == 2 micro-normal perturbation, all of which had nominal audio routing but no listener-pairable musical moment and were reverted in Session 4.5 Phase 0. Before re-introducing any decoration layer (or adding new ones), the one-sentence check is: *"can a listener point at a moment in the music and identify the visual response THIS layer produces?"* If the layer cannot pass that test, do not add it.

## Audio routing notes

Specific audio→visual mappings that must hold (cite D-026 deviation primitives and D-019 stem warmup). Items marked **implemented** are in current code as of V.9 Session 4.5 Phase A; items marked **deferred** were specced under earlier drafts but are not in code:

- **Spike height ← `stems.bass_energy_dev`** (continuous, primary) — **implemented** (`fo_spike_strength`). Fallback: `f.bass_att_rel`. Crossfade via `smoothstep(0.02, 0.06, totalStemEnergy)`. This is the Rosensweig "field strength" parameter; it must respond to bass envelope, not bass beats.
- **Macro Gerstner swell amplitude ← arousal (5s low-pass baseline) + `stems.drums_energy_dev` accent** — **implemented** (`fo_swell_scale`). Arousal sets a sustained baseline amplitude; `drums_energy_dev` adds a smaller short-period crest emphasis. The accent contribution must stay smaller than the baseline so the swell does not read beat-pumped; targeted ratio is baseline ≈70% / accent ≈30% at full energy. The independence of swell amplitude and spike height is load-bearing — calm-body-with-spikes and agitated-body-without-spikes must both be reachable states.
- **§5.8 rig orbit speed ← arousal** (D-126: drives aurora-band azimuth sweep rate in the sky function) — **implemented** (`FerrofluidStageRig.tick`). 5s low-pass smoothing on arousal. At silence the orbit continues at baseline speed so subtle aurora motion persists in the dark-purple sky.
- **§5.8 rig palette phase ← `stems.vocals_pitch_hz` normalized inline with `stems.other_energy_dev` fallback** (D-126: drives per-band aurora hue in the sky function) — **implemented** (`FerrofluidStageRig.computePitchShift`). Same `palette()` phase-shift pattern as `vl_pitchHueShift()` from MV-3c, scaled to ±0.20 via perceptual log-scale normalization over 80 Hz–1 kHz. Confidence gating at `vocals_pitch_confidence >= 0.6`. For instrumental tracks where vocals are absent, `other_energy_dev` carries the fallback. `vocalsPitchNorm` from MV-3c is no longer available — it was retired in DSP.2 S9; the inline normalization is required.
- **§5.8 rig intensity ← `stems.drums_energy_dev`** (D-126: drives per-band aurora brightness in the sky function) — **implemented** (`FerrofluidStageRig`, 150 ms τ smoother). Envelope-tracking, not edge-triggered; bands brighten and dim with drum energy continuously rather than strobing on drum onsets.
- **Thin-film thickness ← arousal** (matID == 2 F0 modulation) — **implemented** (`RayMarch.metal` matID == 2 branch). 220 nm baseline ± 40 nm range → effective [180, 260] nm, inside the subtle blue-to-cyan band; rainbow-oil-slick failure mode lives above ~300 nm.
- **Base sky tint ← D-022 mood valence** (matID == 2 path) — **implemented** (`rm_ferrofluidBaseSky` multiplies its three-stop gradient by `scene.lightColor.rgb`). Negative valence → deeper purple / cooler; positive valence → warmer purple drifting toward magenta. The shift propagates through every mirror-reflected pixel of the substrate, plus through the fog mix toward zenith. (See `07_*` for the static atmospheric character — fog is *material*, not phenomenon.) Note: the original "fog tint multiplies IBL ambient per `iblAmbient *= scene.lightColor.rgb`" mechanism applies to matID == 0 / matID == 3 paths only; matID == 2 bypasses `rm_finishLightingPass` entirely under D-126 and the sky function does the equivalent work.

**Deferred / not currently implemented** (specced under earlier session drafts, omitted in Phase A; revisit if Phase B or Session 5 cert review demands):

- Beat-surface ripple from `stems.drums_beat` rising edges. Omitted because `Scripts/check_drums_beat_intensity.sh` enforces "no drums_beat in intensity scope" per Failed Approach #4 / Audio Data Hierarchy. Ripple as accent on continuous lattice motion can be added in a future increment IF it can be sourced from a continuous-energy primitive rather than from beat edges, AND has a clear musical-role articulation per Authoring Discipline.
- Spike sharpness / surface tension from `stems.vocals_energy_dev`. No surface-tension parameter exists in the current spike profile. Phase B may add one if the linear-cone shape doesn't capture the spike-tip variability the references show.
- Rotational flow direction from `stems.other_energy_dev`. The current geometry uses smooth Voronoi without a domain-warp seed — there is no rotation to apply. If a future increment re-introduces domain warping or particle motion (V.9 Session 4.5b is scoped to introduce particles), this routing returns then.

## Provenance

Curated by: Matt

Image sources:

- `01_macro_ferrofluid_at_swell_scale.jpg` — Getty Images via Unsplash (`kB3R_3slsBc`). Studio ferrofluid macro at field-extending scale. Unsplash License.
- `02_meso_lattice_defects.jpg` — Étienne Desclides via Unsplash (`N2vnFIujxJg`). Studio ferrofluid macro. Unsplash License. *(Retained from pre-redirect set.)*
- `02b_meso_swell_motion_dark_water.jpg` — Ahmed via Unsplash (`kDMu-QmOSLg`). Dark wavy water surface. Unsplash License.
- `02c_meso_excited_water_specular.jpg` — Joshua Earle via Unsplash (`ZN5vm-RW-L4`). Sunlit choppy water surface. Unsplash License.
- `03_micro_spike_surface.jpg` — Robert Stump via Unsplash (`9maYGqtWAnU`), tight crop. Same source as `04_*`. Unsplash License. *(Retained from pre-redirect set.)*
- `03b_micro_droplet_beading.jpg` — Clément Falize via Unsplash (`oOgPgMqTL8A`). Lotus leaf macro. Unsplash License. *(Retained from pre-redirect set.)*
- `04_specular_razor_highlights.jpg` — Robert Stump via Unsplash (`9maYGqtWAnU`), full frame. Unsplash License. *(Retained from pre-redirect set; annotation elevated to dual specular + lighting anchor under the redirect.)*
- `05_anti_chrome_blob_AIGEN.jpg` — **AI-generated** (Gemini / Imagen). Retained as the anti-reference under the **D-065 §(b)** carve-out for AI-generated anti-references in the `05_anti_*` slot, applicable when (a) the depicted failure mode is non-photographable and (b) sourcing a real-photograph or in-engine v1-baseline alternative is impractical. Both conditions hold here: a smooth-chrome-blob aesthetic is non-photographable as a *failure of ferrofluid* (real ferrofluid is never smooth, by definition), and no V.9 v1-baseline frame capture is available pre-implementation. **Replacement plan (per D-065 carve-out requirement):** this image will be replaced with a v1-baseline frame capture of the V.9 implementation once it ships, before V.6 certification. All other images in this folder are real photographs.
- `06_palette_dark_metallic.jpg` — Amith K via Unsplash (`qWaeRQa43RA`). Black sand beach, lower-half crop only. Unsplash License. *(Retained from pre-redirect set.)*
- `07_atmosphere_dark_purple_fog.jpg` — Nathan Anderson via Unsplash (`f98cBlcpy8k`). Stormcloud at night. Cropped to remove the lightning thread visible in the upper-left of the source. Unsplash License. *(Retained from pre-redirect set.)*
- `08_lighting_aurora_over_dark_water.jpg` — Martin Brechtl via Unsplash (`u21fFskAGZQ`). Aurora over still water, Iceland. Unsplash License.
- `10_silence_calm_body.jpg` — Daniela Prušina via Unsplash (`rpWN5gkAbXc`). Calm lake water at rest. Unsplash License.

All Unsplash photographs are CC0-equivalent under the Unsplash License (free for commercial use, no attribution required); attributions provided as courtesy.

**Retired in V.9 redirect (2026-05-13):**

- `01_macro_horizon_dark_coast.jpg` (Christopher La Rocca, Reynisfjara) — horizon framing wrong scale under redirect.
- `01b_macro_mirror_horizon.jpg` (Matheus Oliveira, Salar de Uyuni) — horizon framing wrong scale, daylight palette wrong tone.
- `08_lighting_warm_key_dark_metal.jpg` (Rafael Garcin, Cloud Gate) — single-key + strong-IBL lighting paradigm (§5.2) replaced by §5.8 stage-rig recipe (subsequently amended by D-126 to mirror-reflects-procedural-sky); Anish Kapoor / *Cloud Gate* IP carve-out no longer applicable.
- `09_lighting_caustic_underglow_cyan.jpg` (Michael Worden, underwater) — caustic underlighting removed from §10.3.6 under the redirect.

**Reverted features (V.9 Session 4.5 Phase 0, 2026-05-13 — commit `cda15d47`):**

The following features were implemented in V.9 Session 4 Phase A and reverted in Session 4.5 Phase 0 after M7 review failure (Failed Approach #62 — decoration layers without articulated musical role). The reference images that anchored them are retained in the folder for traceability; future increments may re-introduce these features only with explicit Matt re-authorization and a musical-role articulation per Authoring Discipline.

- **Cassie-Baxter spike-tip droplets** (anchored by `03b_micro_droplet_beading.jpg`). Reverted: `fo_droplet_sdf`, `op_smooth_union` composition, droplet strength field. Reason: no listener-pairable musical moment; read as foreign decoration competing with hero spike signal.
- **Meso domain-warp turbulence** on Voronoi spike lattice (anchored by `02_meso_lattice_defects.jpg`'s defect character — note: the smooth-Voronoi cell-hash offsets now produce equivalent natural non-uniformity, so `02_*` continues to anchor the lattice-defect target, just via the smooth-Voronoi mechanism rather than the explicit warp). Reverted: `fo_meso_warp`, meso strength field, mid_att_rel routing into spike sampling.
- **matID == 2 micro-normal perturbation** (anchored by `03_micro_spike_surface.jpg`). Reverted: `kFerrofluidMicroNormal*` constants, three noiseFBM samples, gradient-derived tangent-space normal addition. Reason: destroyed the substrate's mirror identity by scattering thin-film specular; the layer's claimed "tactile identity" role contradicted the references' razor-sharp-specular character.

**Amended in V.9 Session 4.5 Phase A (2026-05-14):**

- **matID == 2 GPU consumption paradigm** changed from Cook-Torrance per-light loop to mirror-reflects-procedural-sky per D-126. Slot-9 buffer ABI unchanged; the §5.8 stage-rig's audio routing (vocals_pitch → palette, drums_energy_dev → intensity, arousal → orbit speed) is preserved at the rig boundary; the GPU side reinterprets the buffer's per-light fields as aurora-band sky parameters rather than as point-light positions. `08_lighting_aurora_over_dark_water.jpg` is the literal mechanical reference (no longer just an analog).
- **Spike-field height function** switched from regular Voronoi (`voronoi_f1f2`) to Quilez smooth Voronoi (`voronoi_smooth`) per Robert Leitl's published audio-reactive ferrofluid technique. The C¹-continuous height field eliminates cell-boundary normal-flip artifacts; the smooth-Voronoi cell-hash offsets provide the organic non-uniformity that the `02_meso_*` defect-character image anchors.
