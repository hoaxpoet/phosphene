// Session — Playlist connection, preview pipeline, and session state.
// Orchestrates: playlist reading → preview download → stem separation + MIR
// → stem cache → session planning, so visuals are ready from the first frame.

@_exported import Shared
