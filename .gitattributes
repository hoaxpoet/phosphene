# ML weights ship as the ml-weights-v1 GitHub Release asset, fetched by
# Scripts/fetch_weights.sh (PUB.2 cutover) — no longer in git/LFS. Only
# SHA256SUMS is tracked (see .gitignore). Do not re-add *.bin LFS rules here.
# docs/VISUAL_REFERENCES + docs/diagnostics images are gitignored (dev-only
# reference material, no build target uses them) — purged from LFS history to
# stop storage billing. Do not re-add LFS rules here; keep them out of git.

# Append-only narrative log (DOC.8 git-safeguard). When two parallel sessions
# each prepend a release-notes entry they touch the same top-of-file region and
# git conflicts. `merge=union` (built-in driver) keeps BOTH sides' new lines
# instead of conflicting — correct for a prepend-only prose log (worst case:
# two same-session entries interleave in order, which DocIntegrity doesn't gate).
# SAFE ONLY because this file is never edited in place (no tables / no rewrites
# of existing entries — union would duplicate those). Do NOT add structured docs
# with tables here (ENGINEERING_PLAN / KNOWN_ISSUES / CODE_AUDIT stay normal-merge).
docs/RELEASE_NOTES_DEV.md merge=union
